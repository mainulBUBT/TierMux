# Plan: Hey

_Generated by TierMux · 8/4/2026, 8:22:58 PM_

- [ ] `src/agent/clarify.ts` - Contains `parseClarifying()` function that parses `` blocks from model responses. It supports:
- [ ] Format: ``, `Q[Label]: question`, `- option a :: description`, ``
- [ ] Multi-select questions with `Q*[Label]:` syntax
- [ ] Fallback detection for freeform questions (models that ignore the sentinel format)
- [ ] `src/agent/planStructurer.ts` - Contains `extractPlanFromProse()` that classifies model output in Plan mode to determine if it's an actionable plan vs explanation.
- [ ] `src/chatViewProvider.ts` - Has `pendingClarify` state in the Session interface and renders clarifying question cards via `handleRespondToClarify()` (around line 2400).
- [ ] `src/agent/core/tools/ui/question.ts` - Implements `createQuestionTool()` for interactive Q&A mid-turn.
- [ ] Question *parsing* (`parseClarifying`)
- [ ] Question *handling* (showing cards, storing responses)
- [ ] Question *response processing*
- [ ] The system prompt that instructs models to format answers with `` blocks
- [ ] The logic that constructs the question response back into a user turn to continue the conversation
- [ ] Implement the system prompt that generates questioning blocks?
- [ ] Fix the flow that handles clarifying question responses?
- [ ] Implement a new feature for question handling?
