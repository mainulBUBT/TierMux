TierMux কে একই repo থেকে দুইভাবে usable করা — VS Code extension হিসেবে (unchanged) এবং Node library হিসেবে (new `import { Router, runAgentStream, classifyTask, ... } from 'tiermux'`). No behavior change, simple-core invariants intact, no existing test changes। Engine-এর `vscode` leakage পরের PR-এ (Approach B); এই PR-এ শুধু packaging surface।

**New files (10):**
- `src/index.ts` — top-level barrel
- `src/router/index.ts` — router/scoring/capability re-exports
- `src/agent/index.ts` — agent/routing re-exports
- `src/shared/index.ts` — types + WorkReportData re-exports
- `NOTICE` — Apache §4(d) third-party attribution (ai, @ai-sdk/provider, plus verified-Apache deps)
- `tsconfig.lib.json` — declaration emit for the library entries
- `CONTRIBUTING.md` — short build/run/PR guide
- `CHANGELOG.md` — 1.0.0 entry noting OSS-ready surface
- `scripts/librarySmoke.e2e.ts` — headless smoke (esbuild + `node -r ./scripts/vscodeMock.cjs`) proving `import { Router, runPlanStream, classifyTask } from 'tiermux'` works under plain Node
- README "Use as library" section (append-only)

**Modified files (2):**
- `package.json` — add `exports` map (`.`, `./router`, `./agent`, `./providers`, `./shared`, `./package.json`), `files` field, `sideEffects: false`, `keywords`, ensure `repository`/`homepage`/`bugs` present
- `esbuild.js` — add a third build context (entries = 5 barrels, format = cjs+esm, target = node18, external = same as extension) and a `tsc -p tsconfig.lib.json` declaration step; the existing extension build path is untouched

**No-touch list (out of scope, follow-up PRs):**
- `media/src/main.ts` + `media/src/bridge.ts` `@ts-nocheck` removal
- Engine `vscode` decoupling (config reads / fs / workspace root injection)
- Marketplace artwork scan
- `vsce package` step changes

**Verification:**
- `npm run typecheck` passes (no new errors in barrels)
- `npm run typecheck:lib` passes (declaration emit)
- `npm run build` produces both `dist/extension.js` AND `dist/index.{js,mjs,d.ts}` + sub-paths
- `npm run test:e2e:library` (new) passes
- Existing e2e tests (`coreLoop`, `condenseSplit`, `planRunner`, `fitMessages`, `pruneThreshold`, `stepExecutor`, `workReport`, `executionProfile`) all pass unchanged
- `npm pack --dry-run` tarball contains only: `dist/`, `LICENSE`, `NOTICE`, `README.md`, `CHANGELOG.md`, `scripts/vscodeMock.cjs`

**Risk:** Low. Pure additive change. No engine code touched. No call sites modified. Existing `vscodeMock.cjs` pattern (used by all 30+ existing e2e scripts) is the model for library consumer usage.