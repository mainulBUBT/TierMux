{
  "name": "tiermux",
  "displayName": "TierMux",
  "description": "Agentic AI coding assistant powered by the free tiers of 18+ LLM providers (and growing), with automatic failover and smart per-task model routing. Bring your own keys.",
  "version": "0.1.0-beta.2",
  "publisher": "mainul-islam",
  "license": "MIT",
  "preview": true,
  "engines": {
    "vscode": "^1.90.0"
  },
  "categories": [
    "Machine Learning",
    "Programming Languages",
    "Other"
  ],
  "keywords": [
    "ai",
    "llm",
    "agent",
    "copilot",
    "coding assistant",
    "chat",
    "free",
    "openrouter",
    "groq",
    "gemini",
    "mcp",
    "autocomplete"
  ],
  "main": "./dist/extension.js",
  "activationEvents": [],
  "contributes": {
    "viewsContainers": {
      "activitybar": [
        {
          "id": "tiermux",
          "title": "TierMux",
          "icon": "media/icon.svg"
        }
      ]
    },
    "views": {
      "tiermux": [
        {
          "id": "tiermux.chat",
          "name": "Chat",
          "type": "webview"
        }
      ]
    },
    "commands": [
      { "command": "tiermux.newChat", "title": "New Chat", "category": "TierMux", "icon": "$(add)" },
      { "command": "tiermux.showHistory", "title": "Chat History", "category": "TierMux", "icon": "$(history)" },
      { "command": "tiermux.compactChat", "title": "Compact Conversation", "category": "TierMux", "icon": "$(fold)" },
      { "command": "tiermux.openModelSettings", "title": "Manage Models & Keys", "category": "TierMux", "icon": "$(gear)" },
      { "command": "tiermux.setApiKey", "title": "Set API Key", "category": "TierMux" },
      { "command": "tiermux.clearApiKey", "title": "Clear API Key", "category": "TierMux" },
      { "command": "tiermux.explainSelection", "title": "Explain Selection", "category": "TierMux" },
      { "command": "tiermux.fixSelection", "title": "Fix Selection", "category": "TierMux" },
      { "command": "tiermux.refactorSelection", "title": "Refactor Selection", "category": "TierMux" },
      { "command": "tiermux.generateTests", "title": "Generate Tests", "category": "TierMux" },
      { "command": "tiermux.generateDocs", "title": "Generate Docs", "category": "TierMux" },
      { "command": "tiermux.fixWithAI", "title": "Fix with AI", "category": "TierMux" },
      { "command": "tiermux.inlineChat", "title": "Inline Chat (Edit at Cursor)", "category": "TierMux" },
      { "command": "tiermux.addSelectionToChat", "title": "Add Selection to Chat", "category": "TierMux" },
      { "command": "tiermux.reconnectMcp", "title": "Reconnect MCP Servers", "category": "TierMux" },
      { "command": "tiermux.buildIndex", "title": "Build Codebase Index", "category": "TierMux" },
      { "command": "tiermux.clearIndex", "title": "Clear Codebase Index", "category": "TierMux" },
      { "command": "tiermux.refreshModels", "title": "Refresh Model Catalog", "category": "TierMux", "icon": "$(sync)" },
      { "command": "tiermux.generateCommitMessage", "title": "Generate Commit Message", "category": "TierMux", "icon": { "light": "media/icon-commit-light.svg", "dark": "media/icon-commit-dark.svg" } },
      { "command": "tiermux.toggleCompletions", "title": "Toggle Inline Completions", "category": "TierMux" }
    ],
    "menus": {
      "view/title": [
        { "command": "tiermux.newChat", "when": "view == tiermux.chat", "group": "navigation@1" },
        { "command": "tiermux.showHistory", "when": "view == tiermux.chat", "group": "navigation@2" },
        { "command": "tiermux.compactChat", "when": "view == tiermux.chat", "group": "navigation@3" },
        { "command": "tiermux.openModelSettings", "when": "view == tiermux.chat", "group": "navigation@4" }
      ],
      "editor/context": [
        { "submenu": "tiermux.editorSubmenu", "when": "editorHasSelection", "group": "1_freellmagent" }
      ],
      "tiermux.editorSubmenu": [
        { "command": "tiermux.addSelectionToChat", "group": "1@1" },
        { "command": "tiermux.explainSelection", "group": "1@2" },
        { "command": "tiermux.fixSelection", "group": "1@3" },
        { "command": "tiermux.refactorSelection", "group": "1@4" },
        { "command": "tiermux.generateTests", "group": "1@5" },
        { "command": "tiermux.generateDocs", "group": "1@6" }
      ],
      "scm/title": [
        { "command": "tiermux.generateCommitMessage", "when": "scmProvider == git", "group": "navigation" }
      ],
      "commandPalette": [
        { "command": "tiermux.generateCommitMessage", "when": "scmProvider == git" }
      ]
    },
    "submenus": [
      { "id": "tiermux.editorSubmenu", "label": "TierMux" }
    ],
    "keybindings": [
      { "command": "tiermux.inlineChat", "key": "ctrl+i", "mac": "cmd+i", "when": "editorTextFocus" }
    ],
    "configuration": {
      "title": "TierMux",
      "properties": {
        "tiermux.agent.maxIterations": {
          "type": "number",
          "default": 25,
          "description": "Maximum tool-call iterations per agent task before pausing to check in. Higher values let the agent complete multi-step tasks without stopping."
        },
        "tiermux.agent.requireWriteConfirmation": {
          "type": "boolean",
          "default": true,
          "description": "Show a diff and ask for confirmation before the agent writes/creates/deletes a file."
        },
        "tiermux.agent.commandApproval": {
          "type": "string",
          "enum": ["always", "allowlist", "never"],
          "enumDescriptions": [
            "Ask for approval before every command (safest).",
            "Auto-run a small set of safe inspection/test/build commands; ask for anything else.",
            "Disable terminal command execution entirely."
          ],
          "default": "always",
          "description": "How the agent's runCommand tool is gated before running shell commands."
        },
        "tiermux.agent.commandTimeoutMs": {
          "type": "number",
          "default": 120000,
          "description": "Maximum time (ms) a single agent command may run before it is killed."
        },
        "tiermux.agent.commandAllowlist": {
          "type": "array",
          "items": { "type": "string" },
          "default": [],
          "description": "Extra command prefixes auto-runnable in 'allowlist' mode (e.g. \"php artisan migrate\"), in addition to the built-in safe defaults."
        },
        "tiermux.agent.autoCompactThreshold": {
          "type": "number",
          "default": 0.8,
          "description": "Automatically summarize (compact) the conversation when it exceeds this fraction of the model's context window. 0 disables auto-compact."
        },
        "tiermux.context.includeOpenEditors": {
          "type": "boolean",
          "default": true,
          "description": "Automatically include the active file (a slice around the cursor) and open tab names as context each turn."
        },
        "tiermux.utilityModel": {
          "type": "string",
          "default": "auto",
          "description": "Model used for short utility tasks (chat titles, commit messages). 'auto' prefers a strong keyless model so it works without an API key; or pick a specific model in Settings → Others."
        },
        "tiermux.plan.saveToFile": {
          "type": "boolean",
          "default": false,
          "description": "Save plans proposed in Plan mode as markdown checklist files in the workspace. Off by default — the plan and its live task checklist are shown in chat."
        },
        "tiermux.plan.folder": {
          "type": "string",
          "default": "tiermux/plans",
          "description": "Workspace-relative folder where plan files are written (one markdown file per plan)."
        },
        "tiermux.embeddings.rerank": {
          "type": "boolean",
          "default": true,
          "description": "Rerank semantic-search candidates for higher precision (uses Cohere /rerank when a Cohere key is set, otherwise a lexical blend)."
        },
        "tiermux.requestTimeoutMs": {
          "type": "number",
          "default": 60000,
          "description": "Per-provider request timeout (ms) before failing over to the next model."
        },
        "tiermux.rateLimitCooldownMs": {
          "type": "number",
          "default": 60000,
          "description": "How long (ms) to skip a rate-limited provider before trying it again. Honors a provider's Retry-After header when present."
        },
        "tiermux.chat.typingSpeedMs": {
          "type": "number",
          "default": 8,
          "description": "Delay (ms) between chunks of the simulated typing animation. 0 disables animation."
        },
        "tiermux.completions.enabled": {
          "type": "boolean",
          "default": false,
          "description": "Enable Copilot-style inline (ghost-text) completions. Off by default — high request volume against free-tier rate limits."
        },
        "tiermux.completions.model": {
          "type": "string",
          "default": "auto",
          "description": "Model id used for inline completions, or 'auto' to use a fast enabled model."
        },
        "tiermux.completions.debounceMs": {
          "type": "number",
          "default": 350,
          "description": "Debounce (ms) before requesting an inline completion."
        },
        "tiermux.embeddings.enabled": {
          "type": "boolean",
          "default": false,
          "description": "Enable the optional local codebase embeddings index (adds a semantic 'codebaseSearch' tool for the agent). Build it from the ⚙ Models panel."
        },
        "tiermux.embeddings.provider": {
          "type": "string",
          "enum": ["google", "cohere", "mistral", "openrouter", "github", "nvidia"],
          "default": "google",
          "description": "Provider used to embed code chunks (uses that provider's API key). Google text-embedding-004 and Cohere embed have generous free tiers."
        },
        "tiermux.embeddings.model": {
          "type": "string",
          "default": "",
          "description": "Embedding model id (blank = provider default, e.g. text-embedding-004 for Google)."
        },
        "tiermux.embeddings.autoContext": {
          "type": "boolean",
          "default": true,
          "description": "When the index is built, automatically inject the top matching code chunks for your message into each turn (so the agent gets relevant context without calling codebaseSearch)."
        },
        "tiermux.embeddings.batchSize": {
          "type": "number",
          "default": 16,
          "minimum": 1,
          "description": "How many chunks to embed per request while indexing. Larger = fewer requests (helps with per-minute rate limits) but bigger payloads; lower it if the provider rejects large batches."
        },
        "tiermux.embeddings.requestDelayMs": {
          "type": "number",
          "default": 150,
          "minimum": 0,
          "description": "Pause between embedding requests while indexing. Raise this (e.g. 500–1000) if your embedding provider keeps rate-limiting the index build."
        },
        "tiermux.catalog.url": {
          "type": "string",
          "default": "https://docs.google.com/spreadsheets/d/e/2PACX-1vQ0gOSMeX5RCp4zJIZxZRSr74Ro8jYFFlsPqMSm3Uw4hLa4-telxqDwvxxdO6JaXAWL3UM9BaQCTUF5/pub?output=csv",
          "description": "URL to a published CSV (e.g. Google Sheets → File → Share → Publish to web → CSV) that supplies the model catalog. Fetched in the background on startup; the last fetch is cached and used offline, falling back to the built-in catalog. Leave blank to use only the built-in catalog."
        },
        "tiermux.mcpRegistryUrl": {
          "type": "string",
          "default": "",
          "description": "Optional URL to a custom curated MCP list (JSON array or {servers:[…]} in this extension's format). When set, it replaces the bundled 'Add a server' list shown when the search box is empty."
        },
        "tiermux.mcpRegistrySearchUrl": {
          "type": "string",
          "default": "https://registry.modelcontextprotocol.io",
          "description": "Base URL of the MCP registry queried by the remote search box in the MCP panel (official MCP registry by default). Blank disables remote search."
        },
        "tiermux.mcpServers": {
          "type": "object",
          "default": {},
          "markdownDescription": "MCP (Model Context Protocol) stdio servers. Their tools are exposed to the agent. Example:\n\n```json\n\"tiermux.mcpServers\": {\n  \"filesystem\": { \"command\": \"npx\", \"args\": [\"-y\", \"@modelcontextprotocol/server-filesystem\", \".\"] }\n}\n```",
          "additionalProperties": {
            "type": "object",
            "properties": {
              "command": { "type": "string", "description": "stdio: executable to launch (e.g. npx, uvx, node)." },
              "args": { "type": "array", "items": { "type": "string" }, "description": "stdio: arguments." },
              "env": { "type": "object", "additionalProperties": { "type": "string" }, "description": "stdio: extra environment variables." },
              "cwd": { "type": "string", "description": "stdio: working directory (defaults to the workspace root)." },
              "url": { "type": "string", "description": "Remote (streamable-HTTP) server endpoint URL." },
              "headers": { "type": "object", "additionalProperties": { "type": "string" }, "description": "Remote: HTTP headers (e.g. Authorization)." },
              "disabled": { "type": "boolean", "description": "Skip this server." }
            }
          }
        }
      }
    }
  },
  "scripts": {
    "build": "node esbuild.js --production",
    "watch": "node esbuild.js --watch",
    "rebrand": "node scripts/rebrand.mjs",
    "typecheck": "tsc --noEmit",
    "package": "vsce package --no-dependencies"
  },
  "devDependencies": {
    "@types/node": "^20.14.0",
    "@types/vscode": "^1.90.0",
    "@vscode/vsce": "^2.32.0",
    "esbuild": "^0.23.0",
    "highlight.js": "^11.10.0",
    "marked": "^12.0.2",
    "typescript": "^5.5.4"
  },
  "dependencies": {}
}
